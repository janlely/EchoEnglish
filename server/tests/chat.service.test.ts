// Chat service tests are deprecated
// The chat.service.ts has been replaced with conversation.service.ts and contact.service.ts
// New tests should be written for the new services

describe('Chat Service - Deprecated', () => {
  it('should skip chat service tests', () => {
    expect(true).toBe(true);
  });
});
